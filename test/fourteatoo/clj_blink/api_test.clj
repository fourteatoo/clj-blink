(ns fourteatoo.clj-blink.api-test
  (:require [fourteatoo.clj-blink.api :as api]
            [clojure.test :refer :all]))

(deftest a-test
  (testing "No test.  Just compile the dependencies."
    (is true)))
